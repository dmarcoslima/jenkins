/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('Iniciando..') {
            steps { 
                echo 'Iniciando pipeline'
            }
        }

        stage('second stage'){
            steps{
                echo 'Iniciando o segundo estagio'
            }
        }
    }
}